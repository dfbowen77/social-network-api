// Imports the user and thought models
const User = require('./User');
const Thought = require('./Thought');

// Exporting the user and thought models for use in other parts of the application. 
module.exports = { User, Thought };